# Professors Only — saved view.
#
# Selects members whose rank is professor, across any department.

name: Professors Only
description: Members at the rank of professor, across all departments.
source: members
where:
  rank: professor
