/**
 * Abstract — single-page abstract section, conventional in theses.
 *
 * Renders content as a labeled "Abstract" section. The thesis body
 * comes from the section's markdown content; the heading title
 * defaults to "Abstract" but can be overridden via params.
 */
export default {
    defaults: {
        title: 'Abstract',
    },
}
