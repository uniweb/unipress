/**
 * Lemma — math-environment section. Shares numbering with Theorem
 * (the LaTeX preamble declares \\newtheorem{lemma}[theorem]{Lemma}, so
 * Theorem 4.2 followed by Lemma 4.3 numbers correctly).
 */
export default {
    defaults: {
        name: null,
    },
}
