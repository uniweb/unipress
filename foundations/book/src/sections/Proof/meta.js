/**
 * Proof — unnumbered math-environment companion to Theorem.
 *
 * Renders ending with the QED tombstone ▢ (LaTeX's \\qedhere /
 * `proof` env handles this automatically). The body of the section
 * is the proof prose.
 */
export default {
    defaults: {},
}
