/**
 * Default preamble string — `createPreamble()` invoked with default
 * language (English).
 *
 * For parameterised use (localized labels, custom overrides), import
 * `createPreamble` from `./create-preamble`.
 */
import { createPreamble } from './create-preamble.js'

export default createPreamble()
