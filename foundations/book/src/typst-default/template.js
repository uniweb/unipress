/**
 * Default template string — `createTemplate()` invoked with default trim
 * (trade-6x9) and default typography.
 *
 * For parameterised use, import `createTemplate` from `./create-template`
 * and pass `{ trim, typography, structure, labels, language, covers }`.
 */
import { createTemplate } from './create-template.js'

export default createTemplate()
